import styled from '@emotion/styled';

import {css} from "@emotion/react";

const HEADER_STYLE = css`
  margin: 0; padding: 0;
`;

const H1 = styled.h1`
  ${HEADER_STYLE};
  
  font-size: 36px;
`;


export default {
  H1
};
